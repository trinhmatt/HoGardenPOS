import { cartConsts } from "../../constants/cart-constants";

const initState = {};

export const cartReducer = (state = initState, action) => {
    switch (action.type) {
        default:
            return state;
    }
}
