import { cartConsts } from "../../static/constants/cart-constants";

const initState = {};

export default (state = initState, action) => {
    switch (action.type) {
        default:
            return state;
    }
}